---
layout: base
classLayout: page
---

<main class="page__main">
	<article class="content">
		<header class="content__header">
			<h1>{{ title }}</h1>
		</header>
		{{ content }}
	</article>
</main>
<nav class="page__sidebar" aria-label="secondary">
	<ul>
		{% for component in collections.components %}
		<li>
			<a href="{{ component.url }}">{{ component.data.title }}</a>
		</li>
		{% endfor %}
	</ul>
</nav>